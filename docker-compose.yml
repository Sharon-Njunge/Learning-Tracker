version: '3.8'
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: learning_tracker
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  api:
    build: .
    env_file:
      - .env.docker
    environment:
      NODE_ENV: docker   # 🔹 tells NestJS to use .env.docker
    ports:
      - "3000:3000"
    depends_on:
      - db
    command: npm run start:dev

volumes:
  pgdata:
